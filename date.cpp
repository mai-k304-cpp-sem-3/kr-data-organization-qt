#include <iostream>
#include "date.h"

//bool isCorrect(date* _date) {
    /*if ((_date->mon > 0) && (_date->mon < 12)) {
        if ((_date->hour > 0) && (_date->hour < 24)) {
            if ((_date->min > 0) && (_date->min < 60)) {
                if (_date->mon%2 == 0) {
                    if ((_date->day > 0) && (_date->day <= 31)) {
                        return true;
                    } else {
                        return false;
                    }
                } else if (_date->mon%2 == 0) {
                    if ((_date->day > 0) && (_date->day <= 30)) {
                        return true;
                    } else {
                        return false;
                    }
                } else if (_date->mon == 2) {
                    if ((_date->day > 0) && (_date->day <= 28)) {
                        return true;
                    } else {
                        return false;
                    }
                } else {
                    return false;
                }
            } else {
                return false;
            }
        } else {
            return false;
        }
    } else {
        return false;
    }*/
//}
